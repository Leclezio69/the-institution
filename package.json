{
  "name": "the-institution",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A living institutional AI operating simulation.",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "preview": "python3 -m http.server 4173",
    "dev": "npx vercel dev --listen 4173",
    "deploy": "npx vercel --prod"
  },
  "engines": {
    "node": ">=20"
  }
}
